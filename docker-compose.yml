services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: geotrace
      POSTGRES_PASSWORD: password
      POSTGRES_DB: geotrace
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
  